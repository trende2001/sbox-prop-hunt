@using Sandbox;
@using Sandbox.UI;
@using Sandbox.UI.Construct; 
@using System;
@namespace MyGame

@inherits Panel

<style>
	Blind {
    	position: absolute;
    	top: 0;
    	left: 0;
    	width: 100%;
    	height: 100%;
    	opacity: 0;
    	background-color: rgb(0 0 0);
    	z-index: -100;
    	transition: all 0.2s ease-in;
    
    	&.show {
    		opacity: 1;
    	}
    }
</style>

<root>
	
</root>

@code {

	public override void Tick()
	{
		base.Tick();

		Player player = Game.LocalPawn as Player;

		if (player == null)
			return;
		
		//SetClass("show", (player.Team is Seekers && PropHuntGame.Current.RoundState == RoundState.Started ? true : false ));
	}

}